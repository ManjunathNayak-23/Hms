# Starter pipeline
# Start with a minimal pipeline that you can customize to build and deploy your code.
# Add steps that build, run tests, deploy, and more:
# https://aka.ms/yaml

trigger:
- main

pool:
  vmImage: windows-latest

steps:

- task: SSH@0
  inputs:
    sshEndpoint: 'bastin'
    runOptions: 'commands'
    commands: 'ssh -i "vmss_key (2).pem" azureuser@20.119.195.215 cat /var/log/nginx/access.log >> /home/azureuser/nginx.log'
    readyTimeout: '20000'